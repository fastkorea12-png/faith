# 본향 사건파일 — 재미/명확성/실물 준비물 리뷰

리뷰어: 방탈출 기획자 관점 + 대학생 플레이테스트 리드 관점
대상: main 브랜치 현재 상태 (game.js, activity.js, index.html, GAME_DEVELOPMENT_PLAN.md, GAME_PM_BOARD.md, stage-packets/*)
전제: 5팀 동시 진행, 총 2시간 30분 세션

## 1. 재미있는 것 vs 지루한 것

**재미있음**
- 02 숙소: 알림→현장 안내문 대조→사진 진위 판별→메모 복원의 단계적 잠금(`game.js` phone flow)이 실제로 구현돼 있고, "가짜 이름표 vs 진짜 약속"이라는 반전이 명확하다.
- 03 주방: 역할 분담(6종) + 위치 복원 + 문장/권한 대조 3중 근거 추리가 실제 코드(`renderInventoryPuzzleV2`)에 그대로 살아있다. 현재 프로젝트에서 가장 잘 만들어진 스테이지다.
- 04 길: 팀원별 20초 기억 분담 + 루프 반복 + "화살표는 갈 길이 아니라 지나온 기록"이라는 해석 반전이 방탈출다운 긴장감을 만든다.
- 물리 자물쇠를 연 뒤 웹에 같은 답을 또 입력하지 않는 설계 원칙(2.5)이 03/04에서 실제로 지켜지고 있다.

**지루함 / 밋밋함**
- 00 본관: `GAME_DEVELOPMENT_PLAN.md`는 "진술서 카드 5장, 모순 체크 보드, 거짓 기록 3개 찾기"를 요구하지만, 실제 `renderCasePuzzle()`은 문장 하나를 읽고 단어 하나(`나그네`)를 입력하는 것이 전부다. 기획 문서가 경계했던 "교육용 퀴즈"에 가장 가깝다.
- 05 예배당(`renderHomePuzzle`, game.js:489-539): 앞 스테이지 키워드 입력칸이 `localStorage`에서 자동으로 채워져 나온다(game.js:507 `value="${localStorage.getItem(...)}"`). 팀이 기억하거나 카드로 회수할 필요 없이 버튼만 누르면 끝난다. 2시간 30분의 클라이맥스가 사실상 "확인" 버튼 클릭 하나다. PM 보드도 "05 예배당 엔딩/상자 제작"을 여전히 "대기"로 표기한다.
- 01 야외(`renderFieldPuzzle`): 유일하게 `unlock()`을 호출하지 않는 스테이지다. 다른 스테이지는 실제 자물쇠를 연 뒤 화면에서 "코드 확인" 버튼을 눌러 성공 연출(코드 패널)을 받지만, 01은 그런 확인/보상 순간 자체가 없다.
- `game.css`(1873줄) 전체에 `@keyframes`/`transition`이 사실상 없다(진행바 폭 전환 1건 제외). 정답을 맞혀도, 틀려도 화면은 텍스트만 순간적으로 바뀐다. 라이브 이벤트의 정답 공개 순간에 아무런 시청각적 보상이 없다.

## 2. 정확한 모순 / 불분명한 안내

| # | 위치 | 문제 |
|---|---|---|
| 1 | `index.html` 148-183행 "여섯 개의 현장" 카드 | 01~05 스테이지의 정답 키워드(`장막`, `약속`, `청지기`, `더 나은 본향`, 최종 문장)를 플레이 시작 전 메인 페이지에서 그대로 노출한다. 추리 없이 정답을 미리 볼 수 있어 설계원칙 2.1/2.10과 정면으로 배치된다. |
| 2 | `dashboard.js` 41-44행 (ledger hint2) | "부족한 품목은 쌀, 빵, 소금이며 부족분은 4-3-2입니다"라고 되어 있으나, 실제 게임/`stage-packets/stage-03.md`는 품목이 밀·기름·소금이고 수량 계산이 전혀 없다고 명시한다(오히려 "수량을 세거나 부족분을 계산하지 않는다"가 핵심 규칙). 진행자가 이 힌트를 그대로 읽어주면 팀에게 잘못된 정보를 준다. |
| 3 | `config.js` 3행 + `script.js`(qr.html) | 6개 현장 QR이 전부 동일한 고정 토큰(`rodem-2026`)을 사용한다. `activity.js` 174행은 "직접 주소 입력으로는 게임이 열리지 않습니다"라고 안내하지만, 첫 QR을 스캔한 순간 주소창에 이 토큰이 그대로 노출되므로 이후 모든 스테이지를 현장에 가지 않고 URL 조합만으로 열 수 있다. 안내 문구와 실제 동작이 다르다. |
| 4 | `admin-games.js` 3행 vs `game.js`/`activity.js`/PM 보드 | 01 스테이지 제목이 관리자 페이지에서만 "가짜 안식처"로 표시되고, 실제 게임/활동 페이지/PM 보드는 전부 "너무 오래 머문 자리"다. 리허설 때 운영자가 화면을 혼동할 수 있다. |
| 5 | `game.js` 494행 | 05 최종 조합 화면의 03 스테이지 라벨이 "03 식탁"으로 남아 있다(현재 정식 명칭은 "03 주방 및 기타 시설"). |
| 6 | `GAME_PM_BOARD.md` "준비물 한눈에" 표 | 4자리 자물쇠 2개, 방향 자물쇠 1개, 최종 상자 1개처럼 **1세트 기준** 수량만 적혀 있다. 반면 `stage-packets/stage-03.md`는 "다섯 팀에 완전한 독립 키트와 독립 자물쇠"를 명시적으로 요구한다. 5팀 동시 진행이라는 세션 전제와 준비물 표의 수량이 맞지 않는다. |
| 7 | `GAME_DEVELOPMENT_PLAN.md` 05 화면 구성("색상 또는 번호 암호판") 및 PM 보드 자물쇠표(최종 상자 `1116`) | 기획 문서와 PM 보드는 색상/번호 암호판과 실제 `1116` 자물쇠를 요구하지만, 실제 구현·활동 페이지·어떤 stage-packet에도 `1116`이나 암호판 로직이 존재하지 않는다. |

## 3. 스테이지별 필요 실물 소품 / 인쇄물 (5팀분 기준)

| 스테이지 | 현재 문서화 상태 | 5팀 세션에 필요한 것 |
|---|---|---|
| 00 본관 | 전용 stage-packet 없음 | 진술서/시간표/지도조각 카드 세트(기획서가 요구하는 모순 찾기 UI를 구현할 경우), 사건 접수 안내문 5부, QR 카드 |
| 01 야외 | 전용 stage-packet 없음, PM 보드에 자물쇠 2741만 명시 | 표식 4종 × 5세트, 4자리 자물쇠 봉투 × 5, 예비 표식 세트, 진행자 정답표 |
| 02 숙소 | `stage-packets/stage-02.md` 존재(상세) | 문 앞 안내문·이름표 2장·약속카드·포렌식 기록지 × 5세트(패킷에 5팀 운영 항목 없음 — 추가 필요) |
| 03 주방 | `stage-packets/stage-03.md` 존재, 5팀 운영 섹션 포함(모범 사례) | 이미 5세트 요구사항이 명시됨 — 그대로 제작 |
| 04 길 | `stage-packets/stage-04.md` 존재하나 5팀 동시 운영 섹션 없음 | 표식 A~D·문장조각·방향자물쇠·봉투 × 5세트 + 좁은 통로에서 5팀이 겹치지 않을 동선/대기 계획 |
| 05 예배당 | stage-packet 없음, PM 보드 "대기" | 최종 상자(자물쇠 `1116`) 실물 제작, 키워드 카드 5장을 물리적으로 조합하는 보드, 팀별 선언문 출력 |

공통: 5팀용 QR 카드(스테이션당 5장 또는 공용 1장+대기열 운영안), 진행자 통합 정답표 1부, 힌트 1~3단계 운영 카드, 리허설 체크리스트 — 이 세 가지는 어느 문서에도 최종본이 없다.

## 4. 최소 재설계안 (5팀·150분 세션 기준)

1. **05 예배당을 실제 클라이맥스로 재건축**: localStorage 자동 채움 제거, 키워드 카드를 손으로 배치해야 다음 단계가 열리도록 하고, 최종 상자(`1116`)를 물리적으로 열어야 완료 코드가 나오게 한다.
2. **00 본관을 기획서 수준(모순 찾기)으로 복구**하거나, 유지한다면 최소 "거짓 기록 3개 선택" 인터랙션을 추가해 텍스트 입력 하나짜리 구조를 벗어난다.
3. **QR 토큰을 세션/팀 단위로 회전**하거나, 최소한 "직접 주소로 열리지 않는다"는 안내 문구를 실제 동작에 맞게 수정한다.
4. **모든 스테이지에 성공/실패 시각 피드백(전환·강조 애니메이션)을 1종씩 추가**해 정답 공개 순간의 보상감을 만든다.
5. **5팀분 실물 키트 수량을 전 스테이지로 확장**하고 GAME_PM_BOARD 준비물 표를 팀 수 기준으로 재계산한다.
6. **dashboard.js의 잘못된 힌트(03 ledger)를 stage-03.md 정답표와 일치시켜** 라이브 오운영 사고를 막는다.

## 5. 우선순위 구현 백로그

| 우선순위 | 작업 | 수용 기준 |
|---|---|---|
| P0 | dashboard.js ledger hint2 수정 | hint1/hint2가 stage-03.md의 밀/기름/소금·432·수량계산 없음 규칙과 완전히 일치 |
| P0 | 5팀분 실물 준비물 수량 재산정(GAME_PM_BOARD.md) | 6개 스테이지 모두 "5세트 독립 운영" 또는 "대기열 운영안"이 표에 명시됨 |
| P0 | index.html 키워드 스포일러 제거 | 미플레이 상태에서 venue-grid에 정답 키워드가 노출되지 않음 |
| P1 | 05 예배당 실물 상자 + 수기 키워드 조합 재구현 | 새로고침/새 세션에서 키워드 입력칸이 비어 있고, 실제 상자 개방 없이는 완료 코드가 노출되지 않음 |
| P1 | 00 본관 모순 찾기 인터랙션 추가 | 최소 3개 이상의 기록 카드 중 거짓 3개를 선택해야 정체성 입력 단계로 진입 |
| P1 | 성공/실패 애니메이션 1세트 추가 | 모든 `unlock()` 호출과 오답 피드백에 150ms 이상의 시각 전환이 발생 |
| P2 | admin-games.js/game.js 라벨 통일("가짜 안식처"→"너무 오래 머문 자리", "03 식탁"→"03 주방") | 저장소 전체 grep 시 스테이지별 표기가 1개로 통일 |
| P2 | 00/01/05 stage-packet 신규 작성(02~04 수준) | 각 문서에 현장 준비물·정확한 풀이 경로·힌트 3단계·5팀 운영·진행자 정답표 섹션 포함 |
| P2 | QR 토큰 회전 또는 안내 문구 정정 | 활동 페이지 문구와 실제 접근 통제 동작이 일치 |

## 6. 스테이지별 유지/재설계 권고

| 스테이지 | 권고 | 근거 |
|---|---|---|
| 00 본관 | **재설계** | 기획서 대비 구현이 크게 축소됨(카드/모순 찾기 없음, 단어 입력 하나) |
| 01 야외 | **부분 유지** | 리들 텍스트와 실물 탐색 구조는 좋으나, 확인/보상 연출 부재 + 5팀분 키트·전용 패킷 부재 |
| 02 숙소 | **유지** | 이미 잘 구현된 단계적 잠금 포렌식 퍼즐. 5팀 운영 계획만 보강 |
| 03 주방 | **유지(모범 사례)** | 가장 완성도 높은 협동 추리 스테이지. dashboard 힌트 오류만 수정 |
| 04 길 | **부분 유지** | 반전 구조는 훌륭하나 5팀 동시 운영 동선 계획이 없음 |
| 05 예배당 | **재설계(긴급)** | 세션의 클라이맥스가 자동입력+버튼 클릭 수준. PM 보드에도 여전히 "대기"로 표시된 미완성 스테이지 |
